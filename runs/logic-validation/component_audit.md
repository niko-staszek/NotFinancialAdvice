# Component Correctness Audit (Track A)

Method: define (canonical PAC) → read (impl) → trace (hand-computed example) → verdict.
verdict ∈ {correct, suspect, wrong}; action ∈ {none, fix-code, amend-spec}.

| unit | verdict | evidence | action |
|---|---|---|---|
| atr | correct | hand Wilder ATR(3) on 8-bar series = 2.08025, compute_atr returned 2.08025 (full chain matches); confirmed Wilder recursion not simple mean (simple-roll would give 2.0), idx0/1 NaN seed=simple-avg, and prefix-of-6 ATR[5] == full ATR[5] proving no look-ahead. Bar-0 exclusion is a documented caller contract (fn takes only closed bars). | none |
| swing | correct | synthetic rise→drop≥thresh→rise (atr_period=3, mult=1.0): hand expected HIGH@bar6 price14.0 then LOW@bar8 price9.5; detect_swings returned exactly that. Swing-HIGH confirmed only after the ≥(ATR×mult) drop, pivot recorded at the true peak idx not the confirmation bar, swings alternate high/low. | none |
| timeutil | correct | Ran real code: Jan-2026→+1 (PLT 13:00), Jul-2026→+2 (PLT 14:00). Spring-forward computed at last-Sun-Mar=29: 00:59 UTC→+1 (PLT 01:59), 01:00 UTC→+2 (PLT jumps 01:59→03:00) — matches §2.3 "02:00 CET→03:00 CEST". Fall-back at last-Sun-Oct=25 flips +2→+1 at 01:00 UTC (PLT 02:59→02:00). `_last_sunday` is calendar-derived: 2027 returns Mar-28/Oct-31, 2025 Mar-30 (not 2026's dates). All session labels match §2.3 incl. midnight-wrap Asia (02:00/23:00/07:00 PLT→asia), London 08:00/13:59, America 14:00/21:59, Dead 22:00/22:59. | none |
| mmd_clouds | suspect | CLOUD MATH IS CORRECT: traced compute_clouds on real XAUUSD_M5_2025Q1 (17283 bars). At idx1500 all 3 main clouds match independent recompute to FP: orange p48 ema/sma/mid=2668.54498/2667.78604/2668.16551, blue p288=2661.20071/2661.46163/2661.33117, green p1440=2648.35325/2648.86874/2648.61099. `ewm(span=p,adjust=False)`==Pine `ta.ema`, `rolling(p).mean()`==Pine `ta.sma` on close for p∈{48,288,1440} (matches mmd_clouds.pine lines 52/54/56 + Cloud Table). mid=(ema+sma)/2 confirmed. No look-ahead: prefix-of-1501 green_mid==full green_mid. EMA(adjust=False) seeds at close[0] (not NaN), matching Pine recursive ta.ema. WARMUP DEFECT (separate from math): SMA component NaN until idx period-1 — verified orange NaN≤46 valid@47, blue NaN≤286 valid@287, green NaN≤1438 valid@1439; mid NaN follows SMA. Engine warmup gate = cfg.sma_period(61)+_WARMUP_EXTRA(50)=111 (engine.py:71/248/369), FAR below green's 1439. So bars 111–1438 have green_mid=NaN (and 111–286 blue_mid=NaN); mmd_alignment (signals.py:83) degrades to always 'weakened' there — never confirmed/vetoed for ~1300 bars. Cloud math correct; engine warmup gate undersized (fix in LV10). | fix-code |
| mmd_alignment | correct | classify_alignment(orange_mid,blue_mid,green_mid,sentiment): full_bull=O>B>G, full_bear=G>B>O (mmd.py:75-76). Sign sense CORRECT: higher mid = higher price = higher on chart, so O>B>G→"Orange,Blue,Green top→bottom"=Bullish matches MMD_CLOUDS.md Trend Reading table + Directional Reads ("Orange above Blue=momentum up"). Mapping sound: confirmed=stack agrees w/ sentiment, vetoed=stack fully opposite, weakened=mixed/correction/ties/transitional. 7 hand triples ALL matched: clean-bull/bull→confirmed, clean-bull/bear→vetoed, clean-bear/bear→confirmed, clean-bear/bull→vetoed, correction(B,O,G)→weakened (both sents), transitional→weakened; ties/all-equal→weakened (strict > correctly fails). MIDPOINT-vs-TABLE CAVEAT: when bands [min(ema,sma),max(ema,sma)] heavily overlap, mid-ordering can diverge from band-edge stacking (built Case B: mids O=1041>B=1038>G=1037→'confirmed bull', but top-edges order Blue,Orange,Green→a 'correction' read). This is a REAL ambiguity, but MMD_CLOUDS.md defines NO tie-break for overlapping bands; midpoint (center of mass) is a standard monotonic sign-correct proxy. Under-specification in the doc, not an impl faithfulness bug — hence amend-spec to document the overlap tie-break, not a code fix. | amend-spec |
| sentiment | correct | §3.1: bull=close STRICTLY > both EMA21 & SMA61, bear=strictly < both, else transitional. Ran real signals.sentiment (cfg ema=21/sma=61) on 70-bar series: close=110 (ema100.91 sma100.16)→bull; close=90 (ema99.09 sma99.84)→bear; close between MAs (102.45 with ema102.86 sma101.93)→transitional. STRICTNESS verified: flat-100 series so ema==sma==100, close==100 exactly → 'transitional' NOT bull/bear, proving `>`/`<` not `>=`/`<=` (signals.py:55-58). NaN-MA warmup→transitional (line 52). Matches spec exactly incl. "either order of the MAs" (transitional independent of which MA is higher). | none |
| d1_promo_zone | correct | §3.3 prev-calendar-day D1 OHLC. Ran real signals.d1_promo_zone. BULLISH D1 (o100 h110 l95 c105): price97(lower wick Open..Low)→bull_promo, price108(upper wick Close..High)→bear_promo, price102(body)→neutral. BEAR-DAY MIRROR CHECKED CAREFULLY (easy to get wrong): BEARISH D1 (o105 h110 l95 c100): price108(upper wick Open..High)→bear_promo, price97(lower wick Low..Close)→bull_promo, price102(body Close..Open)→neutral — matches §3.3 exactly ("bearish: upper wick Open..High = sellers' promo = bear_promo; lower wick Low..Close = buyers' promo = bull_promo"). Impl uses body_top/body_bot correctly per day polarity (signals.py:144-168). PREV-DAY (not current) lookup confirmed: 2-bar D1 [Jan01 bull, Jan02 huge], current_utc=Jan02, price97 → bull_promo from the Jan01 bar (bar_dates < current_date filter, idxmax of prior, signals.py:134-138), NOT the same-day Jan02 bar. Doji→neutral. v1 never emits first_touch_* (documented v2 deferral) — spec lists them in §3.5 sets so composite handles them; not a bug. | none |
| session_box | suspect | §3.4: box=accumulated session hi/lo (start→current bar), close>box→above, <box→below, inside→inside; narrow filter range<0.5×ATR→inside. SYNTHETIC TRACES (real fn) all pass: clear break above running high→'above'; close within [lo,hi]→'inside'; narrow box (range0.2<0.5×ATR=1.0)→'inside' via filter, and same box w/ ATR0.1 (0.5×ATR=0.05<0.2)→'above' proving the filter (not a window bug) drives the narrow case. 93%-INSIDE CAUSE = **INHERENT-CORRECT, NOT A BUG.** Real-data branch freq replicating engine contract (london/america only, Wilder ATR20, current_session passed) — cross-checked 150 idx vs real fn = 0 mismatches, AND a second run calling the real fn on EVERY bar gave identical numbers: XAUUSD_M5_2025Q1 10574 eval bars→ above6.5%/inside89.1%/below4.4%; of the 89.1% inside, 98.6% is `within_range` (close genuinely between accumulated session hi/lo), narrow_filter only 0.1%, no_session_bars 1.3%. EURUSD_M5_2026-05 1680 eval→ above4.8%/inside91.0%/below4.2%, inside=98.7% within_range / 0.0% narrow. So: window is CORRECT (same-day same-session prior bars), narrow-ATR filter is NOT mis-scaled/over-firing (≤0.1%), above/below comparison WORKS — inside dominates because a single close vs a high-to-low envelope of all prior session bars (wicks included) is inside by construction; clean close-breakout is genuinely rare. SECONDARY DEVIATION (separate from the 93%, real bug): §3.4 says "London box is the primary filter during BOTH London AND America windows", but engine.py:441-443 passes current_session, so during america it builds an AMERICA-only box (resets 14:00 PLT) instead of the London box. Does NOT explain the 93% (would if anything raise breakout rate). Flag fix for the america→London-box requirement. | fix-code |
| composite | correct | §3.5 strict (cfg.direction_strict=True default). Ran real signals.composite_direction. Full bull tuple (sent=bull,mmd=confirmed,d1=bull_promo,box=above)→bull; flipping each single gate to its blocker→neutral: mmd=vetoed→neutral, d1=bear_promo→neutral, box=inside→neutral, sent=transitional→neutral. Non-blocking values pass: mmd=weakened→bull, d1=neutral→bull. Bear mirror (bear/confirmed/bear_promo/below)→bear. mmd_strict=True turns weakened into a blocker: weakened+mmd_strict→neutral (signals.py:281-284). box≠inside requirement is genuinely per §3.5 line 398 (verified). NOTE re Unit 8: since box='inside' ~89-91% of evaluated bars (inherent, correct), the box≠inside gate is the dominant filter killing most directional signals — but it is a LEGITIMATE per-spec gate, not a compounded bug; the high block-rate is inherent breakout-rarity, not a session_box defect. (The america-box §3.4 deviation noted in session_box would shift WHICH bars pass, but composite logic itself is faithful.) | none |
