# Component Correctness Audit (Track A)

Method: define (canonical PAC) → read (impl) → trace (hand-computed example) → verdict.
verdict ∈ {correct, suspect, wrong}; action ∈ {none, fix-code, amend-spec}.

| unit | verdict | evidence | action |
|---|---|---|---|
| atr | correct | hand Wilder ATR(3) on 8-bar series = 2.08025, compute_atr returned 2.08025 (full chain matches); confirmed Wilder recursion not simple mean (simple-roll would give 2.0), idx0/1 NaN seed=simple-avg, and prefix-of-6 ATR[5] == full ATR[5] proving no look-ahead. Bar-0 exclusion is a documented caller contract (fn takes only closed bars). | none |
| swing | correct | synthetic rise→drop≥thresh→rise (atr_period=3, mult=1.0): hand expected HIGH@bar6 price14.0 then LOW@bar8 price9.5; detect_swings returned exactly that. Swing-HIGH confirmed only after the ≥(ATR×mult) drop, pivot recorded at the true peak idx not the confirmation bar, swings alternate high/low. | none |
| timeutil | correct | Ran real code: Jan-2026→+1 (PLT 13:00), Jul-2026→+2 (PLT 14:00). Spring-forward computed at last-Sun-Mar=29: 00:59 UTC→+1 (PLT 01:59), 01:00 UTC→+2 (PLT jumps 01:59→03:00) — matches §2.3 "02:00 CET→03:00 CEST". Fall-back at last-Sun-Oct=25 flips +2→+1 at 01:00 UTC (PLT 02:59→02:00). `_last_sunday` is calendar-derived: 2027 returns Mar-28/Oct-31, 2025 Mar-30 (not 2026's dates). All session labels match §2.3 incl. midnight-wrap Asia (02:00/23:00/07:00 PLT→asia), London 08:00/13:59, America 14:00/21:59, Dead 22:00/22:59. | none |
| mmd_clouds | suspect | CLOUD MATH IS CORRECT: traced compute_clouds on real XAUUSD_M5_2025Q1 (17283 bars). At idx1500 all 3 main clouds match independent recompute to FP: orange p48 ema/sma/mid=2668.54498/2667.78604/2668.16551, blue p288=2661.20071/2661.46163/2661.33117, green p1440=2648.35325/2648.86874/2648.61099. `ewm(span=p,adjust=False)`==Pine `ta.ema`, `rolling(p).mean()`==Pine `ta.sma` on close for p∈{48,288,1440} (matches mmd_clouds.pine lines 52/54/56 + Cloud Table). mid=(ema+sma)/2 confirmed. No look-ahead: prefix-of-1501 green_mid==full green_mid. EMA(adjust=False) seeds at close[0] (not NaN), matching Pine recursive ta.ema. WARMUP DEFECT (separate from math): SMA component NaN until idx period-1 — verified orange NaN≤46 valid@47, blue NaN≤286 valid@287, green NaN≤1438 valid@1439; mid NaN follows SMA. Engine warmup gate = cfg.sma_period(61)+_WARMUP_EXTRA(50)=111 (engine.py:71/248/369), FAR below green's 1439. So bars 111–1438 have green_mid=NaN (and 111–286 blue_mid=NaN); mmd_alignment (signals.py:83) degrades to always 'weakened' there — never confirmed/vetoed for ~1300 bars. Cloud math correct; engine warmup gate undersized (fix in LV10). | fix-code |
| mmd_alignment | correct | classify_alignment(orange_mid,blue_mid,green_mid,sentiment): full_bull=O>B>G, full_bear=G>B>O (mmd.py:75-76). Sign sense CORRECT: higher mid = higher price = higher on chart, so O>B>G→"Orange,Blue,Green top→bottom"=Bullish matches MMD_CLOUDS.md Trend Reading table + Directional Reads ("Orange above Blue=momentum up"). Mapping sound: confirmed=stack agrees w/ sentiment, vetoed=stack fully opposite, weakened=mixed/correction/ties/transitional. 7 hand triples ALL matched: clean-bull/bull→confirmed, clean-bull/bear→vetoed, clean-bear/bear→confirmed, clean-bear/bull→vetoed, correction(B,O,G)→weakened (both sents), transitional→weakened; ties/all-equal→weakened (strict > correctly fails). MIDPOINT-vs-TABLE CAVEAT: when bands [min(ema,sma),max(ema,sma)] heavily overlap, mid-ordering can diverge from band-edge stacking (built Case B: mids O=1041>B=1038>G=1037→'confirmed bull', but top-edges order Blue,Orange,Green→a 'correction' read). This is a REAL ambiguity, but MMD_CLOUDS.md defines NO tie-break for overlapping bands; midpoint (center of mass) is a standard monotonic sign-correct proxy. Under-specification in the doc, not an impl faithfulness bug — hence amend-spec to document the overlap tie-break, not a code fix. | amend-spec |
| sentiment | correct | §3.1: bull=close STRICTLY > both EMA21 & SMA61, bear=strictly < both, else transitional. Ran real signals.sentiment (cfg ema=21/sma=61) on 70-bar series: close=110 (ema100.91 sma100.16)→bull; close=90 (ema99.09 sma99.84)→bear; close between MAs (102.45 with ema102.86 sma101.93)→transitional. STRICTNESS verified: flat-100 series so ema==sma==100, close==100 exactly → 'transitional' NOT bull/bear, proving `>`/`<` not `>=`/`<=` (signals.py:55-58). NaN-MA warmup→transitional (line 52). Matches spec exactly incl. "either order of the MAs" (transitional independent of which MA is higher). | none |
| d1_promo_zone | correct | §3.3 prev-calendar-day D1 OHLC. Ran real signals.d1_promo_zone. BULLISH D1 (o100 h110 l95 c105): price97(lower wick Open..Low)→bull_promo, price108(upper wick Close..High)→bear_promo, price102(body)→neutral. BEAR-DAY MIRROR CHECKED CAREFULLY (easy to get wrong): BEARISH D1 (o105 h110 l95 c100): price108(upper wick Open..High)→bear_promo, price97(lower wick Low..Close)→bull_promo, price102(body Close..Open)→neutral — matches §3.3 exactly ("bearish: upper wick Open..High = sellers' promo = bear_promo; lower wick Low..Close = buyers' promo = bull_promo"). Impl uses body_top/body_bot correctly per day polarity (signals.py:144-168). PREV-DAY (not current) lookup confirmed: 2-bar D1 [Jan01 bull, Jan02 huge], current_utc=Jan02, price97 → bull_promo from the Jan01 bar (bar_dates < current_date filter, idxmax of prior, signals.py:134-138), NOT the same-day Jan02 bar. Doji→neutral. v1 never emits first_touch_* (documented v2 deferral) — spec lists them in §3.5 sets so composite handles them; not a bug. | none |
| session_box | suspect | §3.4: box=accumulated session hi/lo (start→current bar), close>box→above, <box→below, inside→inside; narrow filter range<0.5×ATR→inside. SYNTHETIC TRACES (real fn) all pass: clear break above running high→'above'; close within [lo,hi]→'inside'; narrow box (range0.2<0.5×ATR=1.0)→'inside' via filter, and same box w/ ATR0.1 (0.5×ATR=0.05<0.2)→'above' proving the filter (not a window bug) drives the narrow case. 93%-INSIDE CAUSE = **INHERENT-CORRECT, NOT A BUG.** Real-data branch freq replicating engine contract (london/america only, Wilder ATR20, current_session passed) — cross-checked 150 idx vs real fn = 0 mismatches, AND a second run calling the real fn on EVERY bar gave identical numbers: XAUUSD_M5_2025Q1 10574 eval bars→ above6.5%/inside89.1%/below4.4%; of the 89.1% inside, 98.6% is `within_range` (close genuinely between accumulated session hi/lo), narrow_filter only 0.1%, no_session_bars 1.3%. EURUSD_M5_2026-05 1680 eval→ above4.8%/inside91.0%/below4.2%, inside=98.7% within_range / 0.0% narrow. So: window is CORRECT (same-day same-session prior bars), narrow-ATR filter is NOT mis-scaled/over-firing (≤0.1%), above/below comparison WORKS — inside dominates because a single close vs a high-to-low envelope of all prior session bars (wicks included) is inside by construction; clean close-breakout is genuinely rare. SECONDARY DEVIATION (separate from the 93%, real bug): §3.4 says "London box is the primary filter during BOTH London AND America windows", but engine.py:441-443 passes current_session, so during america it builds an AMERICA-only box (resets 14:00 PLT) instead of the London box. Does NOT explain the 93% (would if anything raise breakout rate). Flag fix for the america→London-box requirement. | fix-code |
| composite | correct | §3.5 strict (cfg.direction_strict=True default). Ran real signals.composite_direction. Full bull tuple (sent=bull,mmd=confirmed,d1=bull_promo,box=above)→bull; flipping each single gate to its blocker→neutral: mmd=vetoed→neutral, d1=bear_promo→neutral, box=inside→neutral, sent=transitional→neutral. Non-blocking values pass: mmd=weakened→bull, d1=neutral→bull. Bear mirror (bear/confirmed/bear_promo/below)→bear. mmd_strict=True turns weakened into a blocker: weakened+mmd_strict→neutral (signals.py:281-284). box≠inside requirement is genuinely per §3.5 line 398 (verified). NOTE re Unit 8: since box='inside' ~89-91% of evaluated bars (inherent, correct), the box≠inside gate is the dominant filter killing most directional signals — but it is a LEGITIMATE per-spec gate, not a compounded bug; the high block-rate is inherent breakout-rarity, not a session_box defect. (The america-box §3.4 deviation noted in session_box would shift WHICH bars pass, but composite logic itself is faithful.) | none |
| signal_candle | correct | §4.1 detect_signal_candle(bar, atr, cfg). Ran real fn (cfg wick_ratio=2.0, range_mult=0.5, close_pct=33). 5 mandated traces ALL match (ATR=10→range floor 5.0): (a) bull OHLC(108,110,100,109) lw=8 body=1 c=109≥floor(100+0.67·10=106.7)→'bullish'; (b) bear OHLC(101,110,100,100) uw=9 body=1 c=100≤ceil(100+0.33·10=103.3)→'bearish'; (c) doji o==c (105,110,100,105) body=0→'none' (guard fires BEFORE ratio div-by-zero, signals.py:331); (d) range 4.0<5.0→'none' (signals.py:334); (e) low wick:body OHLC(101,110,100,109) lw=1 need≥2·body=16→'none'. CLOSE-POS MATH FAITHFUL: pct=33→threshold=0.33; bullish floor = l+(1-0.33)·rng = l+0.67·rng (signals.py:343), bearish ceil = l+0.33·rng (signals.py:347) — exactly §4.1 line 440/444. Wick math correct: lower=min(o,c)-l, upper=h-max(o,c), body=|c-o| (signals.py:337-338). BOUNDARY (f): OHLC(107,112,103,109) lw=4==2·body=4 passes ratio (`>=`) but c=109<floor=109.03→'none' — close-pos gate correctly rejects; confirms `>=` ratio + strict close floor. Both-match priority (returns bullish) is STRUCTURALLY UNREACHABLE for body>0 (needs close in upper AND lower third simultaneously → only at body≈0, caught by doji guard) — dead-but-harmless branch, not a defect. No look-ahead (single-bar pure fn). | none |
| ema_side_confluence | suspect | §4.2 passes_ema_side_rule + §4.3 has_confluence, both run on real fns. EMA-SIDE = CORRECT: bullish c>ema→True, c<ema→False, c==ema→False (STRICT `>`, signals.py:375), bearish mirror (c<ema→True, c==ema→False), 'none'→False, NaN ema→False — exactly §4.2 ("strictly above/below; exact equality rejected"). CONFLUENCE = TWO DEVIATIONS. **(1) WICK-EXTREME DEFECT (real bug, mechanical tests miss it — fix-code):** §4.3 line 470/472 mandates proximity of the REJECTION wick only — `candle.low` for bullish, `candle.high` for bearish. But has_confluence(bar, active_levels, atr, cfg) takes NO signal direction (signals.py:380-385) and computes distance = min(|high-level|, |low-level|) (signals.py:404) — i.e. EITHER wick. Engine call site (engine.py:475-477) passes only (current_bar, active_levels, atr, cfg), so it is NOT compensated upstream. SKEPTIC TRACE: bullish bar low=100/high=110, single level=110 (touches the HIGH = non-rejection wick, 10 away from rejection low) → real fn returns (True, 110.0, 'mm_or_fib_or_cluster'); per §4.3 a bullish signal here MUST be False (rejection wick=low=100 is 10 > threshold 3.0=0.3·ATR10). Impl is MORE PERMISSIVE than spec → admits entries the spec rejects (false-positive risk). Confirmed-good sub-traces: level=102 vs low=100 (dist2≤3)→(True,102.0); far level=80→(False,None,''); two-in-range [101,102.5]→nearest 101.0 wins; empty→(False,None,''); boundary dist==thr (level=103,low=100)→True (`<=` inclusive, signals.py:409). **(2) confluence_type CONSTANT — SPEC DEVIATION but BENIGN (not the row driver):** has_confluence always returns literal "mm_or_fib_or_cluster" (signals.py:410) regardless of which level matched, vs §4.3 line 474 contract confluence_type ∈ {mm_a,mm_b,mm_c,mm_d,fib_retracement,fib_extension,cluster} "identifying which level class produced the match." Impl CANNOT recover the class because active_levels is a bare list[float] with no type tag (spec line 485 intends a {price,type} struct array — type was dropped in v1). confluence_type flows ONLY to the ledger CSV column (engine.py:551, ledger.py:37/64/99) — it NEVER gates any decision (grep: no logic branch reads it; test_ledger.py:33 hard-codes "mm_d" showing the schema expects class-specific values the prod path never emits). So the constant is a reporting-fidelity loss, defensible as v1 simplification → on its own would be amend-spec (downgrade the type contract). Row action driven by defect (1). | fix-code |
| setups | wrong | §6.1–§6.3 state machines (setups.py), all driven through full transitions on real `step_trap`/`step_fail`/`step_spike_channel` + real engine helpers. **MACHINES ARE NOT DEAD — carry-forward WORKS (unlike §5 C-breach/overshoot):** `setup_machines` is created ONCE before the bar loop (engine.py:238) and reassigned each bar via `_setup_machines_for_mms(mms, existing=setup_machines)` which CARRIES FORWARD the live State objects for any MM id already tracked (engine.py:633-635), only minting fresh idle states for new ids. CRITICAL id-stability check (the carry-forward's load-bearing assumption): `MeasuredMove.id` is a positional integer reassigned 1..K each call (targets.py:98/139/164) — but because `detect_swings` confirms/freezes swings append-only, the same MM keeps the same id throughout its window lifetime. PROVEN on a 400-bar synthetic walk (real detect_swings/detect_measured_moves every bar 130..399): **0 id→(a_bar,b_bar,c_bar) reassignments**, ids drift monotonically (14..18 → 23..27) only as the `mms[-N:]` cap (N=5) drops the oldest. Full engine-helper walk (real `_setup_machines_for_mms`+`_step_all_setups`+`_collect_triggered_fires`): trap genuinely stepped idle→first_try_failed→second_try_failed→triggered across multiple bars, carry-forward confirmed (non-idle state survived ≥1 bar for same id = True; 209 non-idle machine-bars). So multi-bar transitions like trap's two tries DO complete in production. **§6.1 TRAP = mostly CORRECT (one minor suspect):** hand trace bull trap_level=100+0.382·10=103.82, atr=1→failure_thr=0.2, max_pen=0.20·10=2.0: low=103.7 (|Δ|0.12≤0.2, pen 0.12≤2.0)→first_try_failed; later bar (bar30, ≤20 window) low=103.75 within 0.2 of first extreme→second_try_failed; bar31 close 104.0, reaction_up=104.0−103.75=0.25≥0.2→triggered. Bear mirror present (setups.py:140-179). SUSPECT sub-point: idle-entry only requires |low−trap_level|≤failure_thr; a bar whose low=104.0 stays 0.18 ABOVE trap_level (never dips INTO the 38.2% zone) is still admitted as first_try_failed — §6.1 wants a failed *break* of the zone, impl accepts mere proximity from above (over-permissive, minor). **§6.2 FAIL = WRONG (real bug, fix-code): SECOND-ATTEMPT SHORTFALL SIGN IS INVERTED.** §6.2 (strategy.md:562/566 "they FAIL TO REACH the same level as the first attempt"; "Second attempt must fail to reach the first attempt's level") = 2nd attempt must be SHALLOWER. For a bull MM (downward correction) shallower ⇒ 2nd low HIGHER than 1st low. Impl (setups.py:248) computes `shortfall = first_attempt_extreme − low` and requires `shortfall >= +0.3·atr`, i.e. `low <= first_extreme − 0.3` ⇒ 2nd attempt goes DEEPER — the exact opposite. SKEPTIC TRACE (real step_fail, fib_382=103.82, thr=0.3): attempt1 low=101.0→first_attempt_done; attempt2 low=102.0 (SHALLOWER, spec-VALID)→stays first_attempt_done (rejected ✗); attempt2 low=100.5 (DEEPER, spec-INVALID)→second_attempt_done (admitted ✗). Bear branch identical inversion (setups.py:290 `shortfall = high − first_extreme >= +thr` requires 2nd high HIGHER = deeper). The Fail setup's *defining* condition is backwards → fix-code. (Geometry otherwise OK: first-attempt min-depth pierce low<=fib_382 correct, trigger close>fib_382 correct; bear fib_382=100+0.382·(−10)=96.18 ✓.) **§6.3 SPIKE&CHANNEL = WRONG (real bug, fix-code): 50%-WICK INVALIDATION IS DEAD/INVERTED-LENIENT.** §6.3 (strategy.md:461 emphatic) "if even a WICK penetrates past 50%, the entire setup is invalidated." Impl pullback_active bull (setups.py:442-448) invalidates ONLY when `low < a_price` (FULL retrace to A), and triggers when `close >= c_price` regardless of how deep a wick dipped below C. SKEPTIC TRACE (real step_spike_channel, A=100,B=116,C=108): bar wick to 105 (well past 50%=C=108, but above A), close 108.5≥C → **triggered** (spec says INVALIDATED). `cfg.pullback_invalidation_fib=0.5` exists (config.py:80) + asserted by test_config — but is referenced ONLY in setups.py comments (lines 337/439/441), NEVER in code; the docstring even mis-claims the line uses `c_price` while the code uses `a_price`. So the setup's most important guard rail is wrong (far more lenient than spec). ALSO unimplemented for S&C: target D=100% A→B expansion from C (§6.3 rule 3) and the 138.2%-of-A→A' exhaustion guard (rules 4-5) — `a_prime_price` is stored (setups.py:373) but no D/exhaustion transition exists. Spike detection itself OK: 3 same-dir bars, net move≥3·atr, ≤1 counter bar → spike_detected with A=close[0]/A'=close[-1] (setups.py:340-383); channel tracks B extreme + C=(A+B)/2, pullback after channel_min_bars=5 (setups.py:404-433). **TRIGGERED-TERMINAL = STALE-LABEL ARTIFACT (suspect, benign):** triggered/invalidated are terminal (setups.py:455) and carry-forward preserves them, so `_collect_triggered_fires` re-reports a triggered MM on EVERY later bar until it leaves the active window — measured up to **49 consecutive bars** in the walk. `winning_setup` flows only to `ledger.setup_type` (engine.py:550, journaling, never a trade gate per module docstring lines 4-6), so the effect is: any entry opened in the ~49-bar window after a trap fires gets stamped `setup_type='trap'` even when that bar's entry is unrelated → stale conviction label, not a trade-decision bug. Benign for v1 but worth noting. Row verdict=wrong driven by §6.2 inverted shortfall + §6.3 dead 50%-wick invalidation; machines are genuinely live (carry-forward sound) so this is NOT the §5 dead-promise class. | fix-code |
| targets | wrong | §5.1–§5.4, all run on real fns. **§5.1 = WRONG, two defects.** **(1) EMA-CROSS USES WICK NOT CLOSE (real bug, mechanical tests miss — fix-code):** §5.1 line 499 + threshold `ema_cross_close_required=true` (line 510) are emphatic — "ends with a CLOSE on the opposite side… wicks that merely touch the EMA do not count — the bar at point B must close on the opposite side." But detect_measured_moves's EMA-side check (targets.py:131/156) tests `swing.price` vs ema, and a swing 'high'/'low' price IS by construction the bar's WICK extreme (swing.py:75/84 store highs[]/lows[]), never its close. The fn has NO access to per-bar close for the cross (sig = bars,swings,ema_series,cfg). SKEPTIC TRACE (real fn): bull triple A(idx10,90,low) B(idx30,110,high) C(idx35,95,low), ema flat 100; then set the B-bar's CLOSE=98 (BELOW ema=100, i.e. wick-only cross) — detect_measured_moves STILL returns the MM (1 MM, valid, D=115) identically to the close-above case. Spec requires reject (B did not close across). Impl more permissive → admits structurally-invalid impulses. Module docstring (targets.py:6-12) self-justifies "swing price IS the EMA-relevant price," but spec's drop-trigger (line 518) says "tighten EMA-cross close requirement," confirming close-cross is curriculum-mandatory → fix-code, not amend-spec. **(2) C-BREACH INVALIDATION NEVER APPLIED ANYWHERE (real bug — fix-code):** §5.1 line 499 + `invalidation_on_c_breach=true` (line 511): "MM is INVALID if price retraces beyond C before reaching D." detect_measured_moves always emits validity='valid' (targets.py:146/170); docstring (targets.py:17-19) defers flip to engine.py — but engine.py only READS mm.validity (engine.py:398/416), NEVER writes it, and re-runs detect_measured_moves from scratch each bar (engine.py:383). No code path in hedgehog/ ever sets validity='invalid' (grep confirms). TRACE (real fn): same bull MM, post-C bars 36–39 plunge low/close to 80 (< C=95, before D=115) → validity stays 'valid'. §5.1 invalidation rule is dead system-wide. **GEOMETRY CORRECT:** bull D=c+(b−a)=95+20=115 ✓ (targets.py:144); bear mirror A(110,high) B(90,low) C(105,high) → D=c−(a−b)=105−20=85 ✓ (targets.py:169); impulse≥1.5×ATR gate + C-vs-A partial-pullback gate present (targets.py:127/135). **§5.2 fib = CORRECT:** A=90,B=110 span=20, real fn returns R[97.64,100.0,102.36]=a+ratio·span for 0.382/0.5/0.618 and E[117.64,122.36,142.36] for 1.382/1.618/2.618 — exact (targets.py:215/219); invalid MM skipped (1 valid+1 invalid → 6 levels, targets.py:209). **§5.2 clusters = CORRECT:** ATR=10 thr=0.3·10=3.0; [100,102,110] → one cluster mean(100,102)=101.0 members[a,b], lone 110 dropped (<min 2) — exact (targets.py:263-271). Documented forward-greedy caveat reproduced: chain [100,102,104,106] (each gap 2≤3) all merge → mean 103.0 though span 6>thr, because the walk compares to last-member not group-anchor (targets.py:257) — matches module docstring, defensible. **§5.3 extended = SUSPECT (sub-part):** math OK — overshoot=0→None, overshoot=3(≥min)→ bull c+1.382·|b−a|=95+27.64=122.64, bear c−1.382·20=77.36 (targets.py:298-306), standard AB=CD external-fib projection from C; spec's "138.2% of original A→B range" is satisfiable as C+ratio·(B−A). BUT mm.overshoot_bars is NEVER incremented anywhere (grep: only default 0 + test fixtures); engine.py:418 calls extended_mm_target on fresh-rebuilt MMs (overshoot_bars=0 always) → the trigger can NEVER fire in production; docstring claim "overshoot_bars updated by engine.py" (targets.py:46) is false → §5.3 is dead path (same class of system-level gap as §5.1 C-breach). **§5.4 settle = CORRECT:** ATR=10 buf=0.5·10=5; long 115→110 (−buf), short 85→90 (+buf) — exact, directions right (targets.py:332-334). Row verdict=wrong driven by §5.1 close-cross + C-breach defects; §5.3 dead-overshoot adds a second fix-code item. | fix-code |
